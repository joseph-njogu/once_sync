import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mini-university',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
